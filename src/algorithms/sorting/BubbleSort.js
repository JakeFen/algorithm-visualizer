// let animations = [];

export const BubbleSort = (numArray) => {
  //   return getAnimations(numArray);
  // };
  // export const getAnimations = (numArray) => {
  //   if (numArray.length <= 1) return numArray;
  //   let swapped = true;
  //   while (swapped) {
  //     swapped = false;
  //     for (let i = 0; i < numArray.length; i++) {
  //       if (numArray[i + 1]) {
  //         numArray[i].style = "bar-selected";
  //         numArray[i + 1].style = "bar-selected";
  //         // Push compared items to change their colors
  //         animations.push(
  //           JSON.parse(JSON.stringify([numArray[i], numArray[i + 1]]))
  //         );
  //         if (numArray[i].value > numArray[i + 1].value) {
  //           swapped = true;
  //           // Swap bars
  //           let temp = numArray[i];
  //           numArray[i] = numArray[i + 1];
  //           numArray[i + 1] = temp;
  //           animations.push(
  //             JSON.parse(JSON.stringify([numArray[i], numArray[i + 1]]))
  //           );
  //         }
  //         numArray[i].style = "";
  //         numArray[i + 1].style = "";
  //         // Push a second time to revert their colors
  //         animations.push(
  //           JSON.parse(JSON.stringify([numArray[i], numArray[i + 1]]))
  //         );
  //       }
  //     }
  //   }
  //   return animations;
};
